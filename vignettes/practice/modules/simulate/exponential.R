# This R script implements the "exponential" module in the practice DSC.
r      <- 0.2
n      <- 1000           # an input. Could specify somewhere else?
X      <- seq(1:n)
y      <- c * (1 + lambda) ^ X 

# keep working on this! 